include std;
fun fib(n) do
    if(n == 0) do
        return 0;
    end
    if(n == 1) do
        return 1;
    end
    return fib(n-1) + fib(n-2);
end

a=fib(10);
print(a);





















































































